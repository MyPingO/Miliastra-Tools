(()=>{
const te=new TextEncoder(),td=new TextDecoder();
const TYPES=[
  {e:0,label:'Integer',typed:11,code:1,kind:'int'},
  {e:1,label:'Floating Point Number',typed:12,code:2,kind:'float'},
  {e:2,label:'String',typed:13,code:3,kind:'string'},
  {e:3,label:'Dynamic Text Field',typed:14,code:4,kind:'dynamic'}
];
const cat=(...xs)=>{let n=0;for(const x of xs)n+=x.length;const o=new Uint8Array(n);let p=0;for(const x of xs){o.set(x,p);p+=x.length}return o};
const vi=n=>{n=BigInt(n);const a=[];while(n>=128n){a.push(Number(n&127n)|128);n>>=7n}a.push(Number(n));return new Uint8Array(a)};
const safe=n=>n<=BigInt(Number.MAX_SAFE_INTEGER)?Number(n):n;
function pv(b,p){let n=0n,s=0n,i=p;while(i<b.length){const x=b[i++];n|=BigInt(x&127)<<s;if(!(x&128))return[safe(n),i];s+=7n;if(s>70n)throw Error('Invalid varint')}throw Error('Truncated varint')}
function parse(b){const fs=[];let p=0;try{while(p<b.length){const[k0,q]=pv(b,p),k=Number(k0),n=k>>>3,w=k&7;p=q;if(!n||![0,1,2,5].includes(w))return null;let v;if(w===0){[v,p]=pv(b,p)}else if(w===1){if(p+8>b.length)return null;v=b.slice(p,p+8);p+=8}else if(w===5){if(p+4>b.length)return null;v=b.slice(p,p+4);p+=4}else{let l;[l,p]=pv(b,p);l=Number(l);if(!Number.isSafeInteger(l)||p+l>b.length)return null;const raw=b.slice(p,p+l);p+=l;v={raw,fields:null};const sub=parse(raw);if(sub)v.fields=sub}fs.push({n,w,v})}return p===b.length?fs:null}catch{return null}}
function encField(f){const k=vi((f.n<<3)|f.w);if(f.w===0)return cat(k,vi(f.v));if(f.w===1||f.w===5)return cat(k,f.v);const body=f.v.fields?enc(f.v.fields):f.v.raw;return cat(k,vi(body.length),body)}
const enc=fs=>cat(...fs.map(encField));
const str=f=>{if(!f||f.w!==2)return null;try{return td.decode(f.v.raw)}catch{return null}};
const f0=(fs,n,w)=>fs.find(f=>f.n===n&&(w==null||f.w===w));
const all=(fs,n,w)=>fs.filter(f=>f.n===n&&(w==null||f.w===w));
function walk(fs,cb,path=[]){cb(fs,path);fs.forEach((f,i)=>{if(f.w===2&&f.v.fields)walk(f.v.fields,cb,[...path,i])})}
function floatBytes(value){const b=new Uint8Array(4);new DataView(b.buffer).setFloat32(0,Number(value)||0,true);return b}
function floatValue(b){return b instanceof Uint8Array&&b.length===4?new DataView(b.buffer,b.byteOffset,b.byteLength).getFloat32(0,true):0}
function typeInfo(e){return TYPES.find(t=>t.e===Number(e))||TYPES[0]}
function defaultValue(e){const t=typeInfo(e);return t.kind==='int'||t.kind==='float'?0:''}
function makeLenField(n,fields){return{n,w:2,v:{raw:new Uint8Array(),fields}}}
function makeVarField(n,v){return{n,w:0,v}}
function makeRawField(n,w,raw){return{n,w,v:raw}}
function definitionInfo(f){if(f.n!==501||f.w!==2||!f.v.fields)return null;const type=f0(f.v.fields,501,0),name=f0(f.v.fields,502,2),id=f0(f.v.fields,503,0);const text=str(name);if(!id||text==null)return null;const e=type?Number(type.v):0;if(e<0||e>3)return null;return{id:Number(id.v),name:text,typeEnum:e}}
function buildDefinition(v,index){const fs=[];if(Number(v.typeEnum)!==0)fs.push(makeVarField(501,Number(v.typeEnum)));fs.push({n:502,w:2,v:{raw:te.encode(v.name||`Variable ${index}`),fields:null}});fs.push(makeVarField(503,index));return makeLenField(501,fs)}
function valueWrapper(typeEnum,value){const t=typeInfo(typeEnum);let inner;
  if(t.kind==='int')inner=makeVarField(501,Math.max(0,Math.trunc(Number(value)||0)));
  else if(t.kind==='float')inner=makeRawField(501,5,floatBytes(value));
  else if(t.kind==='dynamic')inner=makeLenField(501,[{n:501,w:2,v:{raw:te.encode(String(value??'')),fields:null}}]);
  else inner={n:501,w:2,v:{raw:te.encode(String(value??'')),fields:null}};
  return [makeLenField(t.typed,[inner]),makeVarField(501,t.code),makeLenField(502,[makeVarField(501,18446744073709551615n)])]
}
function readValue(wrapper,typeEnum){const t=typeInfo(typeEnum);try{const tf=f0(wrapper,t.typed,2),vf=f0(tf.v.fields,501);if(t.kind==='int')return Number(vf.v);if(t.kind==='float')return floatValue(vf.v);if(t.kind==='dynamic'){const nested=f0(vf.v.fields,501,2);return str(nested)??''}return str(vf)??''}catch{return defaultValue(typeEnum)}}
function buildPair(v,index,value){return makeLenField(501,[makeVarField(501,index),makeLenField(502,valueWrapper(v.typeEnum,value))])}
function readPairs(dataField,formals){const byId=new Map();if(dataField?.v?.fields)for(const p of all(dataField.v.fields,501,2)){if(!p.v.fields)continue;const id=f0(p.v.fields,501,0),wrap=f0(p.v.fields,502,2);if(id&&wrap?.v.fields)byId.set(Number(id.v),wrap.v.fields)}return formals.map(v=>byId.has(v.id)?readValue(byId.get(v.id),v.typeEnum):defaultValue(v.typeEnum))}
function replaceRepeated(fs,n,w,replacements){const idx=[];fs.forEach((f,i)=>{if(f.n===n&&f.w===w)idx.push(i)});if(idx.length){fs.splice(idx[0],idx.length,...replacements)}else{fs.unshift(...replacements)}}
function itemInfo(fs){const id=f0(fs,503,0),refs=f0(fs,505,2),nameWrap=f0(fs,501,2),data=f0(fs,502,2);if(!id||!refs||!nameWrap?.v.fields)return null;const nameField=f0(nameWrap.v.fields,501,2);const name=str(nameField);if(!name)return null;const out=[];if(refs.v.fields)for(const r of refs.v.fields){if(r.n!==501||r.w!==2||!r.v.fields)continue;const x=f0(r.v.fields,501,0);if(x)out.push(Number(x.v))}return{name,id:Number(id.v),refs:out,fs,nameField,refsField:refs,dataField:data,values:[]}}
function findFormalContainer(root){const candidates=[];walk(root,fs=>{const f34=f0(fs,34,2);if(!f34?.v.fields||!f0(fs,501,0)||!f0(fs,502,0)||!f0(fs,503,0)||!f0(fs,504,2))return;const defs=all(f34.v.fields,501,2).map(definitionInfo).filter(Boolean);candidates.push({field:f34,defs})});if(!candidates.length)return null;return candidates.sort((a,b)=>b.defs.length-a.defs.length)[0]}
function findModel(payload){const root=parse(payload);if(!root)return null;let best=null;walk(root,fs=>{const items=fs.map(itemInfo).filter(Boolean);if(items.length<2)return;if(!best||items.length>best.items.length)best={root,container:fs,items}});if(!best)return null;const fc=findFormalContainer(root);best.formalField=fc?.field||null;best.formals=(fc?.defs||[]).sort((a,b)=>a.id-b.id);best.items.forEach(item=>item.values=readPairs(item.dataField,best.formals));return best}
function statusFile(bytes){if(bytes.length<24)return null;const payload=bytes.slice(20,-4),m=findModel(payload);if(!m)return null;let has=false;walk(m.root,fs=>{for(const f of fs)if(f.w===2&&str(f)==='Status Display Area')has=true});return has?{payload,m}:null}
function refField(id){return makeLenField(501,[makeVarField(501,Number(id))])}
function setRefs(item,refs){item.refs=[...refs];item.refsField.v.fields=refs.map(refField);item.refsField.v.raw=new Uint8Array()}
function syncFormals(){const m=state.model;m.formals.forEach((v,i)=>v.id=i+1);if(m.formalField){replaceRepeated(m.formalField.v.fields,501,2,m.formals.map((v,i)=>buildDefinition(v,i+1)));m.formalField.v.raw=new Uint8Array()}
  for(const item of m.items){while(item.values.length<m.formals.length)item.values.push(defaultValue(m.formals[item.values.length].typeEnum));if(item.values.length>m.formals.length)item.values.length=m.formals.length;if(item.dataField?.v?.fields){const pairs=m.formals.map((v,i)=>buildPair(v,i+1,item.values[i]??defaultValue(v.typeEnum)));replaceRepeated(item.dataField.v.fields,501,2,pairs);item.dataField.v.raw=new Uint8Array()}}
}
function buildFile(state){syncFormals();const payload=enc(state.model.root),header=new Uint8Array(state.bytes.slice(0,20)),view=new DataView(header.buffer);view.setUint32(0,20+payload.length,false);view.setUint32(16,payload.length,false);return cat(header,payload,state.bytes.slice(-4))}
function hex(n){return'0x'+Number(n).toString(16).toUpperCase().padStart(8,'0')}
function parseId(v){const s=String(v).trim();if(!s)return null;const n=/^0x/i.test(s)?parseInt(s,16):parseInt(s,10);return Number.isSafeInteger(n)&&n>=0?n:null}
let state=null,overlay=null;
function css(){if(document.getElementById('statusDisplayStyle'))return;const s=document.createElement('style');s.id='statusDisplayStyle';s.textContent=`.sd-overlay{position:fixed;inset:0;z-index:300;background:#090d16;color:#f4f7fb;display:grid;grid-template-rows:auto 1fr auto;font:13px/1.45 Inter,system-ui,sans-serif}.sd-head{display:flex;align-items:center;gap:8px;padding:10px 14px;background:#0e1623;border-bottom:1px solid #28364b}.sd-head h2{font-size:15px;margin:0 10px 0 0}.sd-file{margin-left:auto;color:#8395ad;font:11px ui-monospace,monospace}.sd-main{overflow:auto;padding:20px}.sd-wrap{max-width:1180px;margin:auto}.sd-section{margin-bottom:24px}.sd-section h3{font-size:11px;text-transform:uppercase;letter-spacing:.08em;color:#8496ae;margin:0 0 9px}.sd-note{color:#778ba5;font-size:11px;margin:5px 0 10px}.sd-card{border:1px solid #293950;background:#111a28;border-radius:10px;margin:7px 0;padding:11px}.sd-item-head{display:grid;grid-template-columns:minmax(180px,1fr) 90px auto;gap:10px;align-items:center}.sd-item-name{font-weight:700}.sd-id{font:11px ui-monospace,monospace;color:#91a5bf}.sd-refs,.sd-values{margin-top:9px;display:flex;flex-direction:column;gap:6px}.sd-subtitle{font-size:10px;text-transform:uppercase;letter-spacing:.06em;color:#7488a2;margin-top:10px}.sd-ref{display:grid;grid-template-columns:minmax(180px,1fr) 130px auto;gap:7px;align-items:center}.sd-ref input,.sd-formal input,.sd-formal select,.sd-value input{background:#0b121e;border:1px solid #30405a;color:#f4f7fb;border-radius:7px;padding:6px 8px}.sd-ref code{color:#8fa5c0}.sd-empty{color:#6f8199;padding:6px 0}.sd-formal{display:grid;grid-template-columns:70px minmax(180px,1fr) 210px auto;gap:8px;align-items:center;margin:6px 0}.sd-value{display:grid;grid-template-columns:minmax(160px,.8fr) minmax(220px,1fr);gap:8px;align-items:center}.sd-value label{color:#91a5bf}.sd-actions{display:flex;gap:5px}.sd-status{padding:7px 14px;background:#0c131f;border-top:1px solid #243146;color:#8193aa}.sd-overlay button{border:1px solid #34445f;background:#172033;color:#eef3f8;border-radius:7px;padding:6px 9px;cursor:pointer}.sd-overlay button:hover:not(:disabled){background:#202d42}.sd-overlay button:disabled{opacity:.35;cursor:not-allowed}.sd-overlay button.primary{background:#2f6fd0;border-color:#4387df}.sd-overlay button.danger{color:#ff9ba5;background:transparent;border-color:#49313a}.sd-formal-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.sd-pill{font-size:10px;color:#9fb5cf;border:1px solid #32445e;border-radius:999px;padding:2px 6px}`;document.head.append(s)}
function render(){css();overlay?.remove();overlay=document.createElement('div');overlay.className='sd-overlay';overlay.innerHTML=`<div class="sd-head"><h2>Status Display Area</h2><button data-close>Back to editor</button><button class="primary" data-export>Export edited GIA</button><span class="sd-file"></span></div><div class="sd-main"><div class="sd-wrap"><section class="sd-section"><div class="sd-formal-head"><div><h3>Formal Variables</h3><div class="sd-note">Uses the same Formal Variable definition/value format as other UI components.</div></div><button data-add-formal>+ Formal Variable</button></div><div data-formals></div></section><section class="sd-section"><h3>Status Item Configuration</h3><div class="sd-note">Each item can link zero, one, or multiple Unit Status configuration IDs. Formal Variable values are shown per list item.</div><div data-items></div></section></div></div><div class="sd-status">Experimental Formal Variable editing enabled. Export a copy and test it in Miliastra.</div>`;document.body.append(overlay);overlay.querySelector('.sd-file').textContent=state.file.name;
const formalBox=overlay.querySelector('[data-formals]');
const renderFormals=()=>{formalBox.innerHTML='';if(!state.model.formalField){formalBox.innerHTML='<div class="sd-empty">Formal Variable configuration was not detected in this file.</div>';overlay.querySelector('[data-add-formal]').disabled=true;return}if(!state.model.formals.length)formalBox.innerHTML='<div class="sd-empty">No Formal Variables.</div>';state.model.formals.forEach((v,index)=>{const r=document.createElement('div');r.className='sd-formal';r.innerHTML=`<code class="sd-id">ID ${index+1}</code><input maxlength="20"><select></select><div class="sd-actions"><button data-up>↑</button><button data-down>↓</button><button class="danger" data-remove>Remove</button></div>`;const input=r.querySelector('input'),sel=r.querySelector('select');input.value=v.name;TYPES.forEach(t=>{const o=document.createElement('option');o.value=t.e;o.textContent=t.label;sel.append(o)});sel.value=String(v.typeEnum);input.addEventListener('input',()=>{v.name=input.value.slice(0,20);syncFormals();renderItems()});sel.addEventListener('change',()=>{const next=Number(sel.value);if(next===v.typeEnum)return;v.typeEnum=next;state.model.items.forEach(item=>item.values[index]=defaultValue(next));syncFormals();renderItems()});r.querySelector('[data-up]').disabled=index===0;r.querySelector('[data-down]').disabled=index===state.model.formals.length-1;r.querySelector('[data-up]').addEventListener('click',()=>moveFormal(index,-1));r.querySelector('[data-down]').addEventListener('click',()=>moveFormal(index,1));r.querySelector('[data-remove]').addEventListener('click',()=>{state.model.formals.splice(index,1);state.model.items.forEach(item=>item.values.splice(index,1));syncFormals();renderFormals();renderItems()});formalBox.append(r)})};
function moveFormal(index,dir){const target=index+dir;if(target<0||target>=state.model.formals.length)return;[state.model.formals[index],state.model.formals[target]]=[state.model.formals[target],state.model.formals[index]];state.model.items.forEach(item=>[item.values[index],item.values[target]]=[item.values[target],item.values[index]]);syncFormals();renderFormals();renderItems()}
overlay.querySelector('[data-add-formal]').addEventListener('click',()=>{const i=state.model.formals.length;state.model.formals.push({id:i+1,name:`Variable ${i+1}`.slice(0,20),typeEnum:0});state.model.items.forEach(item=>item.values.push(0));syncFormals();renderFormals();renderItems()});
const itemsBox=overlay.querySelector('[data-items]');
const renderItems=()=>{itemsBox.innerHTML='';state.model.items.sort((a,b)=>b.id-a.id).forEach(item=>{const card=document.createElement('div');card.className='sd-card';card.innerHTML=`<div class="sd-item-head"><span class="sd-item-name"></span><span class="sd-id"></span><button data-add>+ Unit Status</button></div><div class="sd-subtitle">Link Unit Status</div><div class="sd-refs"></div><div class="sd-subtitle">Formal Variable Values</div><div class="sd-values"></div>`;card.querySelector('.sd-item-name').textContent=item.name;card.querySelector('.sd-id').textContent='Item '+item.id;const refs=card.querySelector('.sd-refs');const redrawRefs=()=>{refs.innerHTML='';if(!item.refs.length)refs.innerHTML='<div class="sd-empty">No linked Unit Statuses</div>';item.refs.forEach((id,index)=>{const row=document.createElement('div');row.className='sd-ref';row.innerHTML='<input><code></code><button class="danger">Remove</button>';const input=row.querySelector('input'),code=row.querySelector('code');input.value=hex(id);code.textContent=String(id);input.addEventListener('change',()=>{const n=parseId(input.value);if(n===null){input.value=hex(item.refs[index]);return}const next=[...item.refs];next[index]=n;setRefs(item,next);input.value=hex(n);code.textContent=String(n)});row.querySelector('button').addEventListener('click',()=>{const next=item.refs.filter((_,i)=>i!==index);setRefs(item,next);redrawRefs()});refs.append(row)})};redrawRefs();card.querySelector('[data-add]').addEventListener('click',()=>{setRefs(item,[...item.refs,0]);redrawRefs();const ins=refs.querySelectorAll('input');ins[ins.length-1]?.focus()});const vals=card.querySelector('.sd-values');if(!state.model.formals.length)vals.innerHTML='<div class="sd-empty">No Formal Variables.</div>';state.model.formals.forEach((v,index)=>{const row=document.createElement('div');row.className='sd-value';row.innerHTML='<label></label><input>';row.querySelector('label').innerHTML=`${v.name} <span class="sd-pill">${typeInfo(v.typeEnum).label}</span>`;const input=row.querySelector('input');const t=typeInfo(v.typeEnum);input.value=String(item.values[index]??defaultValue(v.typeEnum));if(t.kind==='int'||t.kind==='float')input.type='number';if(t.kind==='int')input.step='1';if(t.kind==='float')input.step='any';input.addEventListener('change',()=>{item.values[index]=t.kind==='int'?Math.max(0,Math.trunc(Number(input.value)||0)):t.kind==='float'?(Number(input.value)||0):input.value;syncFormals();input.value=String(item.values[index])});vals.append(row)});itemsBox.append(card)})};
renderFormals();renderItems();
overlay.querySelector('[data-close]').addEventListener('click',()=>{overlay.remove();overlay=null;state=null});overlay.querySelector('[data-export]').addEventListener('click',()=>{try{const bytes=buildFile(state);downloadBytes(state.file.name.replace(/\.gia$/i,'')+' - edited.gia',bytes);overlay.querySelector('.sd-status').textContent=`Exported ${state.model.items.length} Status Item Configuration entries and ${state.model.formals.length} Formal Variables.`}catch(err){console.error(err);overlay.querySelector('.sd-status').textContent=`Export failed: ${err.message||err}`}})}
const input=document.getElementById('fileInput');if(input)input.addEventListener('change',async e=>{const file=input.files?.[0];if(!file)return;e.stopImmediatePropagation();try{const bytes=new Uint8Array(await file.arrayBuffer()),found=statusFile(bytes);if(found){state={file,bytes,model:found.m};render();input.value='';return}await openFile(file)}catch(err){console.error(err);alert(`Could not open file:\n\n${err.message||err}`);input.value=''}},true);
})();